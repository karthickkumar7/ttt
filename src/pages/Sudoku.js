import React from "react";

const Sudoku = () => {
  return (
    <div className="h-screen w-screen flex justify-center items-center">
      <svg height={400} width={500} className="bg-slate-300"></svg>
    </div>
  );
};

export default Sudoku;
